# ─────────────────────────────────────────────────────────────────────────────
# requirements.txt — Projet 6 : Bitcoin Time Series (Streamlit Cloud)
# ─────────────────────────────────────────────────────────────────────────────

# ── Data Science core ─────────────────────────────────────────────────────────
numpy>=1.26,<2.0
pandas>=2.2,<3.0
scipy>=1.13,<2.0

# ── Visualisation ─────────────────────────────────────────────────────────────
plotly>=5.22,<6.0
# matplotlib et seaborn retirés : non utilisés dans le dashboard Streamlit

# ── Séries temporelles — Statistiques ─────────────────────────────────────────
statsmodels>=0.14,<1.0
# pmdarima retiré : Auto-ARIMA non utilisé dans app.py

# ── Prophet ───────────────────────────────────────────────────────────────────
prophet>=1.1.5,<2.0
# pystan retiré : prophet>=1.1 utilise cmdstanpy par défaut, plus léger

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn>=1.5,<2.0
xgboost>=2.0,<3.0

# ── Deep Learning ─────────────────────────────────────────────────────────────
tensorflow-cpu>=2.16,<3.0
# tensorflow-cpu au lieu de tensorflow : ~600MB gagnés, pas de GPU sur Streamlit Cloud

# ── Dashboard Streamlit ───────────────────────────────────────────────────────
streamlit>=1.35,<2.0
# watchdog retiré : inutile sur Streamlit Cloud (utile seulement en dev local)

# ── Utilitaires ───────────────────────────────────────────────────────────────
python-dotenv>=1.0,<2.0
joblib>=1.4,<2.0
# tqdm retiré : non utilisé dans app.py
# jupyter/jupyterlab/ipykernel/nbformat retirés : inutiles sur Streamlit Cloud
# fastapi/uvicorn/pydantic/httpx retirés : API séparée, pas dans le dashboard
